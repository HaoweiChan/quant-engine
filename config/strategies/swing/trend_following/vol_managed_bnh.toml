# Vol-Managed B&H — refactored 2026-04-16
# Pure signal emitter — contract sizing handled by PortfolioSizer via
# exposure_multiplier metadata. Only the 5 signal params live here.
# Shared params for TX and MTX (single param set, both instruments validated)
# Previously validated: real TX 2024-06-01–2026-04-10 Sharpe=1.67 alpha=1.06 MDD=34.7%
# Previously validated: real MTX 2024-06-01–2026-04-10 Sharpe=1.62 alpha=1.12 MDD=40.3%

[params]
vol_lookback_days = 10    # 10-day realized vol — more reactive, better timing
trend_sma_days    = 20    # 20-day SMA gate — trend filter
dd_breaker_pct    = 0.15  # exit overlay when DD >= 15% AND below SMA
dd_reentry_pct    = 0.05  # re-enter overlay when DD recovers to 5%
vol_target_annual = 0.20  # 20% vol target for overlay sizing

[optimization]
level       = 1
level_name  = "L1_EXPLORATORY"
achieved_at = "2026-04-11T00:00:00+08:00"
holding_period = "swing"

[optimization.gate_results]
tx_sharpe       = 1.667
tx_alpha        = 1.060
tx_mdd_pct      = 34.7
mtx_sharpe      = 1.621
mtx_alpha       = 1.120
mtx_mdd_pct     = 40.3
sensitivity_cv  = 0.0
mc_strong_bull_p50_sharpe = 1.08
mc_sideways_p50_sharpe    = 2.93
